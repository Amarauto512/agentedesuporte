services:
  - type: web
    name: agente-gemini
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: bash start.sh
    envVars:
      - key: GOOGLE_API_KEY
        value: SUA_CHAVE_AQUI
